# 网申本地填表工具

这套工具把流程拆成两段：AI 只处理表格结构、公开职业信息和岗位适配；本机脚本才读取姓名、证件号、联系方式、住址和家庭信息等字段。AI 永远不会接触 `profile.ini`、`.vault` 或已经替换完成的最终表格。

## 文件说明

- `local_fill_form.py`：准备脱敏版、检查占位符、本地替换和加密资料库。
- `field_map.json`：当前《国企网申模板 个人信息.docx》的字段位置映射，不包含个人值。
- `profile.ini.example`：本地资料库初始模板。脚本会按文档占位符自动补齐变量，不要上传。
- `AI任务指引.md`：每次可以直接交给 AI 的固定任务说明，不含个人信息。
- 你的 Word 模板和 AI 输入/输出文件：只保存在本机，不纳入公开仓库。

## 一次性准备

在工具目录打开 PowerShell。若电脑还没有依赖，执行：

```powershell
python -m pip install lxml cryptography
```

复制资料库模板并填写真实值：

```powershell
Copy-Item .\profile.ini.example .\profile.ini
notepad .\profile.ini
```

`=` 两边的空格可有可无；推荐保留 `KEY = 值` 的形式，阅读更清楚。`profile.ini` 和 `personal.vault` 只保存在这个工具目录，不要把它发给 AI、上传到网盘或放进 Git 仓库。

### 自动同步变量（无需手工维护变量名）

`prepare`、`fill` 都会读取 Word 中出现的 `{{变量名}}`，自动把 `profile.ini` 缺少的变量追加为 `变量名 =`。这不会删除注释、不会调整已有行、更不会覆盖你已经填过的真实值；终端只报告新增字段数量，不显示字段值。

正确顺序是：先运行一次下面的 `prepare`，再打开 `profile.ini` 填写新出现的空变量。之后即使 AI 针对另一份表格新增了占位符，直接运行 `fill` 也会先同步；若有新字段尚未填写，脚本会停下来列出字段名，不会生成半成品。

如果已有 AI 输入版或 AI 返回版，想先单独同步而不填写，运行：

```powershell
python .\local_fill_form.py sync-profile `
  --input .\AI返回.docx `
  --profile .\profile.ini
```

记事本打开 `profile.ini` 时，脚本对文件的追加不会自动写回你尚未保存的编辑内容。请先按 `Ctrl+S` 保存自己的修改；脚本同步后，关闭并重新打开文件（或在记事本提示时选择重新加载），即可看到新变量。不要同时在记事本和脚本中编辑同一行。

更推荐立刻生成加密资料库：

```powershell
python .\local_fill_form.py vault `
  --profile .\profile.ini `
  --output .\personal.vault
```

密码由脚本提示输入，不会显示，也不会写入命令行历史。确认 `.vault` 能正常使用后，再按自己的数据清理习惯处理明文 `profile.ini`；不要在没有备份或没有验证 `.vault` 之前删除它。

## 每次申请岗位的操作流程

### 可选：启动后台本地填表服务

如果希望 AI 完成公开内容后自动触发本地替换，可以先在本机启动服务：

```powershell
.\start_fill_service.ps1
```

服务启动时只输入一次 Vault 密码，密码只通过标准输入传给后台进程，不写入命令行和日志。之后把 AI 返回的、仍含 `{{...}}` 占位符的 DOCX 放入 `automation\inbox`。服务会自动把完整表格写到 `automation\completed`，并在 `automation\status` 写入不含敏感值的处理状态。停止服务：

```powershell
.\stop_fill_service.ps1
```

后台服务只是自动化入口，不是操作系统级隔离。为保证 Agent 即使有终端权限也无法查看敏感文件，必须把 Agent 运行在无权读取 `personal.vault` 和 `automation\completed` 的独立账户、沙箱或虚拟机中；否则只能保证正常工作流不主动读取敏感文件，不能保证越权命令无法读取。

### 1 生成给 AI 的版本

将你自己的 Word 模板放在仓库外的本地目录后，执行：

```powershell
python .\local_fill_form.py prepare `
  --input "D:\你的本地目录\个人信息模板.docx" `
  --output .\AI输入版.docx `
  --map .\field_map.json
```

`prepare` 只会把原模板中本来为空的个人、联系方式和家庭信息单元格改为 `{{FULL_NAME}}`、`{{ID_NUMBER}}` 等占位符。模板中已有的内容一律保持原样，黄色字段也保持原样，由 AI 根据岗位直接改写。

这一步也会自动同步 `field_map.json` 同目录的 `profile.ini`。如需使用别处的资料库，在命令末尾添加 `--profile D:\安全目录\profile.ini`。

把 `AI任务指引.md`、公司的空白申请表和 `AI输入版.docx` 一起交给 AI。务必把文件名或消息写清楚：空白申请表是“目标申请表（唯一输出模板）”，`AI输入版.docx` 是“个人信息参考表（只供取值）”。AI 不应复制参考表的版式或表格。

```text
请根据岗位描述处理这份 Word 网申信息表。

规则：
1. 只使用表格中已有的教育经历、工作经历、项目经历、证书、奖励、自我评价和我另外提供的岗位描述。
2. 将公开职业信息按岗位做必要的压缩、改写、日期格式统一和字段映射。
3. 黄色字段必须根据具体岗位和公司填写，例如期望城市、行业、岗位、意向部门、面试地点、特长和兴趣爱好。
4. 所有形如 {{...}} 的占位符都属于本地私密字段。不要猜测、改名、删除、展开或替换它们；如果不知道字段含义，只保持原样。
5. 不要索要身份证号、护照号、手机号、邮箱、家庭成员姓名、住址、银行信息或任何其他敏感值。
6. 保留原 Word 表格结构、合并单元格、字体、颜色和段落换行。输出一份新的 Word 文件，并在回复中只说明仍未解决的占位符名称，不要复述任何敏感内容。
```

让 AI 返回 `AI返回.docx` 后，不要把它和 `profile.ini` 或 `.vault` 一起上传、压缩或发送。

### 2 在本机替换敏感字段

推荐使用加密资料库：

```powershell
python .\local_fill_form.py fill `
  --input .\AI返回.docx `
  --output .\完整信息表.docx `
  --vault .\personal.vault
```

如果暂时没有建立 `.vault`，也可以使用明文资料库：

```powershell
python .\local_fill_form.py fill `
  --input .\AI返回.docx `
  --output .\完整信息表.docx `
  --profile .\profile.ini
```

脚本只有在所有占位符都能从本地资料库找到非空值时才会生成最终文件；缺失字段时不会生成半成品。替换过程直接处理 Word 的 XML，通常能保留原表格、图片和版式；跨 Word 文本块拆开的占位符也会尝试合并替换。

使用 `.vault` 时，`fill` 也会在输入密码后把新变量写入加密资料库（值为空、已有值不变）。然后按报出的字段名更新资料库，再重新加密或使用明文 `profile.ini` 维护即可。

## 最终人工检查清单

打开 `完整信息表.docx`，提交前逐项检查：

- 仍搜索一次 `{{`，确认没有占位符。
- 姓名、证件号码类型、证件号码、出生日期、学历、是否应届是否一致。
- 手机、邮箱、通讯地址、邮编和紧急联系人是否正确。
- 黄色字段是否真正对应这家公司、岗位、城市和面试地点；黄色字段由 AI 直接改写，不应保留为占位符。
- 薪资、到岗时间、是否服从调剂、意向部门是否按公告要求填写；当前模板中“期望薪资/到岗时间”布局较特殊，建议始终手工核对。
- 当前源模板有几处“标签存在、值单元格缺失”的排版：性别、最高学位、是否应届、英语掌握程度等字段不由 `prepare` 自动写入；最终表格必须人工补齐。期望薪资也建议人工填写。
- 日期格式、教育经历顺序、工作经历起止日期和“无/是/否”选项是否符合该网站要求。
- 最后再另存一份提交版，不要把带有完整敏感信息的文件上传给任何 AI 工具。

## 安全边界和维护

- `profile.ini` 和 `.vault` 是秘密；`AI输入版.docx`、`AI返回.docx` 可以包含职业信息，但不应包含真实敏感值。
- 密码、银行账户、验证码和登录凭据不应放进这套资料库，也不要让 AI 代填。
- 如果网站字段名称变化，优先在 AI 输出中手动添加现有占位符；只有当前模板的行列结构稳定时，才修改 `field_map.json`。
- 其他表格可直接使用 `{{PASSPORT_NO}}`、`{{BANK_ACCOUNT}}` 等占位符，并在本地资料库中添加同名键。脚本只会替换实际出现的占位符。
- 这个脚本不会连接网络、浏览器或招聘网站；它只读写你指定的本地文件。
