# 网申本地填表工具

这套工具把流程拆成两段：AI 只处理表格结构、公开职业信息和岗位适配；本机脚本才读取姓名、证件号、联系方式、住址和家庭信息等字段。AI 永远不会接触 `profile.ini`、`.vault` 或已经替换完成的最终表格。

## 文件说明

- `local_fill_form.py`：准备脱敏版、检查占位符、本地替换和加密资料库。
- `field_map.json`：当前《国企网申模板 个人信息.docx》的字段位置映射，不包含个人值。
- `profile.ini.example`：本地资料库模板。复制后填写，不要上传。
- 你的 Word 模板和 AI 输入/输出文件：只保存在本机，不纳入公开仓库。

## 一次性准备

在工具目录打开 PowerShell。若电脑还没有依赖，执行：

```powershell
python -m pip install lxml cryptography
```

复制资料库模板并填写真实值：

```powershell
Copy-Item .\profile.ini.example .\profile.ini
notepad .\profile.ini
```

`profile.ini` 只保存在本机。不要把它发给 AI、上传到网盘或放进 Git 仓库。

更推荐立刻生成加密资料库：

```powershell
python .\local_fill_form.py vault `
  --profile .\profile.ini `
  --output .\personal.vault
```

密码由脚本提示输入，不会显示，也不会写入命令行历史。确认 `.vault` 能正常使用后，再按自己的数据清理习惯处理明文 `profile.ini`；不要在没有备份或没有验证 `.vault` 之前删除它。

## 每次申请岗位的操作流程

### 1 生成给 AI 的版本

将你自己的 Word 模板放在仓库外的本地目录后，执行：

```powershell
python .\local_fill_form.py prepare `
  --input "D:\你的本地目录\个人信息模板.docx" `
  --output .\AI输入版.docx `
  --map .\field_map.json
```

`prepare` 会把个人基本信息、联系方式、家庭情况改为 `{{FULL_NAME}}`、`{{ID_NUMBER}}` 等占位符，也会把黄色岗位字段改为 `{{TARGET_CITY}}`、`{{TARGET_ROLE}}` 等占位符。它不会改变工作经历、教育经历、项目经历和自我评价等公开职业信息。

把 `AI输入版.docx` 交给 AI 时，同时给它岗位描述或招聘公告。可以直接使用下面的指令：

```text
请根据岗位描述处理这份 Word 网申信息表。

规则：
1. 只使用表格中已有的教育经历、工作经历、项目经历、证书、奖励、自我评价和我另外提供的岗位描述。
2. 将公开职业信息按岗位做必要的压缩、改写、日期格式统一和字段映射。
3. 黄色字段必须根据具体岗位和公司填写，例如期望城市、行业、岗位、意向部门、面试地点、特长和兴趣爱好。
4. 所有形如 {{...}} 的占位符都属于本地私密字段。不要猜测、改名、删除、展开或替换它们；如果不知道字段含义，只保持原样。
5. 不要索要身份证号、护照号、手机号、邮箱、家庭成员姓名、住址、银行信息或任何其他敏感值。
6. 保留原 Word 表格结构、合并单元格、字体、颜色和段落换行。输出一份新的 Word 文件，并在回复中只说明仍未解决的占位符名称，不要复述任何敏感内容。
```

让 AI 返回 `AI返回.docx` 后，不要把它和 `profile.ini` 或 `.vault` 一起上传、压缩或发送。

### 2 检查 AI 是否漏填黄色岗位内容

```powershell
python .\local_fill_form.py check `
  --input .\AI返回.docx `
  --map .\field_map.json `
  --kind yellow
```

这个命令只检查黄色岗位字段；个人信息占位符本来就应该保留，所以不会被列出来。这里不会读取本地资料库，也不会显示真实值。

### 3 在本机替换敏感字段

推荐使用加密资料库：

```powershell
python .\local_fill_form.py fill `
  --input .\AI返回.docx `
  --output .\完整信息表.docx `
  --vault .\personal.vault
```

如果暂时没有建立 `.vault`，也可以使用明文资料库：

```powershell
python .\local_fill_form.py fill `
  --input .\AI返回.docx `
  --output .\完整信息表.docx `
  --profile .\profile.ini
```

脚本只有在所有占位符都能从本地资料库找到非空值时才会生成最终文件；缺失字段时不会生成半成品。替换过程直接处理 Word 的 XML，通常能保留原表格、图片和版式；跨 Word 文本块拆开的占位符也会尝试合并替换。

## 最终人工检查清单

打开 `完整信息表.docx`，提交前逐项检查：

- 仍搜索一次 `{{`，确认没有占位符。
- 姓名、证件号码类型、证件号码、出生日期、学历、是否应届是否一致。
- 手机、邮箱、通讯地址、邮编和紧急联系人是否正确。
- 黄色字段是否真正对应这家公司、岗位、城市和面试地点。
- 薪资、到岗时间、是否服从调剂、意向部门是否按公告要求填写；当前模板中“期望薪资/到岗时间”布局较特殊，建议始终手工核对。
- 当前源模板有几处“标签存在、值单元格缺失”的排版：性别、最高学位、是否应届、英语掌握程度等字段不由 `prepare` 自动写入；最终表格必须人工补齐。期望薪资也建议人工填写。
- 日期格式、教育经历顺序、工作经历起止日期和“无/是/否”选项是否符合该网站要求。
- 最后再另存一份提交版，不要把带有完整敏感信息的文件上传给任何 AI 工具。

## 安全边界和维护

- `profile.ini` 和 `.vault` 是秘密；`AI输入版.docx`、`AI返回.docx` 可以包含职业信息，但不应包含真实敏感值。
- 密码、银行账户、验证码和登录凭据不应放进这套资料库，也不要让 AI 代填。
- 如果网站字段名称变化，优先在 AI 输出中手动添加现有占位符；只有当前模板的行列结构稳定时，才修改 `field_map.json`。
- 其他表格可直接使用 `{{PASSPORT_NO}}`、`{{BANK_ACCOUNT}}` 等占位符，并在本地资料库中添加同名键。脚本只会替换实际出现的占位符。
- 这个脚本不会连接网络、浏览器或招聘网站；它只读写你指定的本地文件。
