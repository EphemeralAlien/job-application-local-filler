# AI 网申填表任务指引

本任务通常有两份 Word 文件。它们的角色绝对不能混淆：

- `【目标申请表】...docx`：唯一的输出模板。最终 Word 必须以它为基础填写。
- `【个人信息参考】...docx`：只提供公开经历和 `{{占位符}}`，绝不是输出模板。

目标申请表决定最终文件的每一页、表格、合并单元格、图片、字体、颜色、行高、列宽和字段顺序。个人信息参考表的版式、表格、页码和标题不得复制到输出中。

如果用户没有明确标出哪个文件是“目标申请表”、哪个是“个人信息参考”，先询问，不要猜测，更不要任选其中一份作为输出。

## 用户每次只需发送的指令

上传或选中本文件、目标申请表、个人信息参考表和岗位说明后，用户只需说：

```text
《【目标申请表】xxx.docx》是唯一输出模板；《【个人信息参考】xxx.docx》只供取值。请按《AI任务指引.md》开始填写。
```

如果申请表本身已经包含全部公开经历和占位符，可以不提供个人信息参考表。

## 必须执行的填写流程

1. 以目标申请表为唯一输出基础，在原有单元格、文本框和字段中填写；不要新建另一份信息表，不要复制参考表的页面或表格。
2. 只从个人信息参考表提取公开的教育、工作、项目、证书、奖励、技能和自我评价内容，并映射到目标申请表对应字段。必要时可压缩、改写和转换日期格式，但不得改变事实。
3. 对姓名、证件号、电话、邮箱、地址、家庭成员等私密字段，复制参考表中语义对应的 `{{变量名}}` 占位符到目标申请表；不得填写、猜测、删除、改名或展开占位符。
4. 黄色或岗位适配字段依据本次公司、岗位和招聘公告填写；不要沿用上一次申请的公司、岗位、城市、部门或面试地点。
5. 招聘公告没有提供、参考表也没有提供的信息，在目标申请表中保持空白；如有必要，只能在对用户的回复中列为待确认项，不得编造。

## 可靠推理、补全与留空

- 对任何目标申请表字段，只要个人信息参考表、招聘公告和用户补充资料能够通过确定的计算、格式转换、字段映射或上下文关系得出唯一且可验证的结果，就应自动推理并填写；不要只因为参考表没有一模一样的字段就放弃处理。
- 年龄只是示例：参考表提供出生年月或出生年份时，应自动换算年龄；完整生日按当前日期计算，只有年份时按当前年份减出生年份计算。其他可唯一推导的日期、经历时长、规范名称或对应字段，也按同一原则处理。
- 任何字段只要资料未提供、推导结果不唯一、需要主观判断，或存在合理但不同的多种答案，相关单元格必须保持真正空白。不要在表内写“需用户确认”“待补充”“暂无”或任何提示文字；可在对用户的回复中简短列出空白项。
- 不得按常识补全、估计或编造。

## 输出前必须自检

- 输出文件的版式和结构来自目标申请表，而不是个人信息参考表。
- 目标申请表原有的表格、图片、合并单元格、字段顺序、字体和颜色均被保留。
- 个人信息参考表的标题、页码、表格和样式没有出现在输出文件中。
- 所有 `{{...}}` 占位符拼写和大小写保持不变。
- 输出为新的 `.docx` 文件，不覆盖目标申请表原文件。

## 隐私与权限规则

- 不要索要、推测或复述姓名、身份证号、护照号、手机号、邮箱、住址、出生日期、家庭成员信息、银行信息、密码或验证码。
- 不要查看、搜索、读取、复制、上传或尝试解密 `profile.ini`、`personal.vault`、密码文件或最终完整申请表。
- 正常工作流不需要打开敏感文件。敏感文件只能由固定的本地填表服务读取，不能作为 AI 的阅读材料。
- 不要为了确认、调试或检查而主动编写或运行读取敏感文件的脚本；尤其不要运行 `Get-Content`、`type`、`cat`、Python 文件读取、压缩、搜索或目录枚举命令。
- AI只可把仍含占位符的结果投递到 `automation\inbox`。本地服务完成后，只报告处理状态；不要打开 `automation\completed` 中的完整表格。
